<section id="featured" class="bg">
    <!-- start slider -->


    <!-- start slider -->
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <!-- Slider -->
                <div id="main-slider" class="main-slider flexslider">
                    <ul class="slides">
                        {% for rs in slider %}
                        <li>
                            <img src="{{ asset('uploads/images/' ~ rs.image) }}" alt="" style="width: 1024px; height: 360px;" />
                            <div class="flex-caption">
                                <h3>{{ rs.title }}</h3>
                                <p>{{ rs.description }}</p>
                                <a href="/packet/{{ rs.id }}" class="btn btn-theme"> Paket Detayları</a>
                            </div>
                        </li>
                        {% endfor %}

                    </ul>
                </div>
                <!-- end slider -->
            </div>
        </div>
    </div>


</section>
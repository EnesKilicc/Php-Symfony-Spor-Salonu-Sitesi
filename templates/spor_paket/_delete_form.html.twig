<form method="post" action="{{ path('user_spor_paket_delete', {'id': spor_paket.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ spor_paket.id) }}">
    <button class="btn">Delete</button>
</form>

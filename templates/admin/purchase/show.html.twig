{% extends 'adminbase.html.twig' %}

{% block title %}Edit Category{% endblock %}

{% block body %}
<div class="dashboard-wrapper">
    <div class="container-fluid dashboard-content">
        <div class="row">
            <div class="col-xl-12 col-lg-12 col-md-12 col-sm-12 col-12">
    <h1>Purchase</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ purchase.id }}</td>
            </tr>
            <tr>
                <th>Userid</th>
                <td>{{ purchase.userid }}</td>
            </tr>
            <tr>
                <th>Paketid</th>
                <td>{{ purchase.paketid }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ purchase.name }}</td>
            </tr>
            <tr>
                <th>Surname</th>
                <td>{{ purchase.surname }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ purchase.email }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ purchase.phone }}</td>
            </tr>
            <tr>
                <th>Duration</th>
                <td>{{ purchase.duration }}</td>
            </tr>
            <tr>
                <th>Total</th>
                <td>{{ purchase.total }}</td>
            </tr>
            <tr>
                <th>Ip</th>
                <td>{{ purchase.ip }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ purchase.note }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ purchase.status }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ purchase.createdAt ? purchase.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Updated_at</th>
                <td>{{ purchase.updatedAt ? purchase.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>



    <a href="{{ path('admin_purchase_edit', {'id': purchase.id}) }}">edit</a>

    {{ include('admin/purchase/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
</div>
{% endblock %}